{   "variables": {
        "azure_subscription_id": "{{env `AZURE_SUBSCRIPTION_ID`}}",
        "azure_tenant_id": "{{env `AZURE_TENANT_ID`}}",
        "azure_client_id": "{{env `AZURE_CLIENT_ID`}}",
        "azure_client_secret":"{{env `AZURE_CLIENT_SECRET`}}"
    },
    "builders": [{
        "type": "azure-arm",
        "subscription_id": "{{user `azure_subscription_id`}}",
        "tenant_id": "{{user `azure_tenant_id`}}",
        "client_id": "{{user `azure_client_id`}}",
        "client_secret": "{{user `azure_client_secret`}}",

        "resource_group_name": "cloudsquanch_rg",
        "storage_account": "cloudsquanchc53e67",

        "capture_container_name": "custom-images",
        "capture_name_prefix": "packer",

        "os_type": "Linux",
        "image_publisher": "canonical",
        "image_offer": "ubuntuserver",
        "image_sku": "16.04.0-LTS",

        "azure_tags": {
          "dept": "engineering"
        },

        "location": "centralus",
        "vm_size": "Standard_DS1_V2"
    }],
    "provisioners": [
      {
        "type": "file",
        "source": "../../../consul",
        "destination": "/tmp"
      },
      {
        "type": "file",
        "source": "../../../nomad",
        "destination": "/tmp"
      },
      {
        "type": "file",
        "source": "../../../vault",
        "destination": "/tmp"
      },
      {
        "type": "shell",
        "environment_vars": [
          "VAULT_ENT={{user `vault_ent_url`}}"
        ],
        "scripts": [
          "../../../shared/base.sh",
          "../../../consul/scripts/consul_install.sh",
          "../../../nomad/scripts/nomad_install.sh",
          "../../../vault/scripts/vault_install.sh"
        ]
      },
      {
        "type": "shell",
        "inline": [
          "echo \"Installing systemd services for Ubuntu\"",
          "sudo cp /tmp/consul/files/consul.service /lib/systemd/system/consul.service",
          "sudo cp /tmp/vault/files/vault.service /lib/systemd/system/vault.service",
          "sudo cp /tmp/nomad/files/nomad.service /lib/systemd/system/nomad.service",
          "sudo chmod 664 /lib/systemd/system/consul*",
          "sudo chmod 664 /lib/systemd/system/vault*",
          "sudo chmod 664 /lib/systemd/system/nomad*"
        ]
      },  
     {
      "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E sh '{{ .Path }}'",
      "inline": [
        "apt-get update",
        "apt-get upgrade -y",
        "/usr/sbin/waagent -force -deprovision+user && export HISTSIZE=0 && sync"
      ],
      "inline_shebang": "/bin/sh -x",
      "type": "shell"
    }
  ]
}
